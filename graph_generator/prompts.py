"""Prompt templates for Gemini calls."""

FILE_SUMMARY_PROMPT = """あなたはソースコードアナリストです。以下のソースコードファイルを分析し、日本語で簡潔な要約（2-4段落）を作成してください。

以下の点を含めてください：
- ファイルの目的と役割
- 主要なクラスとその責務
- 重要なメソッド/関数とその動作
- 依存関係（インポート）と他ファイルとの関連
- 使用されているデザインパターン

ファイル: {file_name}

```
{content}
```

日本語で要約を記述してください。出力に要約以外余計なことを書かないでください。"""

FILE_CHUNK_SUMMARY_PROMPT = """あなたはソースコードアナリストです。以下は大きなソースコードファイルの一部（チャンク {chunk_index}/{total_chunks}）です。
このチャンクの内容を日本語で簡潔に要約してください（1-2段落）。

ファイル: {file_name}

```
{content}
```

このチャンクに含まれるクラス、メソッド、主要なロジックを要約してください。日本語で記述。出力に要約以外余計なことを書かないでください。"""

FILE_MERGE_SUMMARY_PROMPT = """あなたはソースコードアナリストです。以下は大きなソースコードファイルの各チャンクの要約です。
これらを統合し、ファイル全体の包括的な要約（2-4段落）を日本語で作成してください。

ファイル: {file_name}（全{total_chunks}チャンク、{total_lines}行）

{chunk_summaries}

以下の点を含めてください：
- ファイルの目的と役割
- 主要なクラスとその責務
- 重要なメソッド/関数とその動作
- 依存関係と他ファイルとの関連
- 使用されているデザインパターン

日本語で要約を記述してください。出力に要約以外余計なことを書かないでください。"""

DIR_SUMMARY_PROMPT = """あなたはソフトウェアアーキテクトです。以下のディレクトリ内のファイル要約とサブディレクトリ要約を統合し、
このディレクトリ/モジュールの全体的な要約（2-3段落）を日本語で作成してください。

ディレクトリ: {dir_name}

### 子ファイルの要約:
{child_file_summaries}

### 子ディレクトリの要約:
{child_dir_summaries}

以下の点を含めてください：
- このディレクトリ/モジュールの全体的な目的
- 内部のファイルがどのように連携しているか
- 主要なコンポーネントとその相互作用
- このレベルで見られるパターンやアーキテクチャ上の判断

日本語で要約を記述してください。出力に要約以外余計なことを書かないでください。"""

TOPIC_EXTRACTION_PROMPT = """あなたはソフトウェアアーキテクトです。以下のファイル要約一覧から、コードベースの機能モジュールを3-8個識別してください。

モジュールは機能・特徴別に分類してください（アーキテクチャ層別ではなく）。
良い例: "テストフレームワークコア", "アサーション機能", "テストランナー"
悪い例: "サービス層", "データアクセス層"

### ファイル要約:
{summaries}

以下のJSON形式で返してください（日本語のモジュール名）:
[
  {{
    "name": "モジュール名（日本語）",
    "linked_files": ["File1.php", "File2.php"],
    "subtopics": [
      {{"name": "サブトピック名（日本語）", "linked_files": ["File1.php"]}}
    ]
  }}
]

CRITICAL: Return ONLY valid JSON. Each file should appear in at least one module."""

TOPIC_SUMMARY_PROMPT = """あなたはテクニカルライターです。以下のモジュールについて、包括的なドキュメントを日本語で作成してください。

モジュール名: {topic_name}

### 関連ファイルの内容:
{file_contents}

### ファイルの要約:
{file_summaries}

以下のセクションを含むMarkdownドキュメントを作成してください：

# {topic_name}

## はじめに
このモジュールの役割とアプリケーション内での位置づけ。

## アーキテクチャと設計
使用されているアーキテクチャパターンを説明。

### コンポーネント関係図
```mermaid
classDiagram
    class ClassName {{
        +type fieldName
        +returnType methodName()
    }}
```

### モジュールコンポーネント
| コンポーネント | 責務 |
|-----------|----------------|
| **ClassName** | 何をするか |

## データフロー
```mermaid
sequenceDiagram
    participant User
    participant Component
    User->>Component: アクション
```

## 主要コンポーネント
各重要クラスについて説明。

## 依存関係
他モジュールへの依存。

日本語で記述してください。クラス名・メソッド名はそのまま使用。"""

TOPIC_MERGE_PROMPT = """以下の複数チャンクから抽出されたトピックリストを、3-15個のグローバルトピックに統合してください。
重複を除去し、関連するトピックを統合してください。

入力トピック:
{topics_json}

JSON配列形式で返してください（日本語のモジュール名）:
[{{"name": "モジュール名", "linked_files": ["file1.php"], "subtopics": [{{"name": "サブトピック", "linked_files": ["file1.php"]}}]}}]

CRITICAL: Return ONLY valid JSON."""

DB_SCHEMA_PROMPT = """あなたはデータベース設計に詳しいテクニカルライターです。
以下のスキーマ定義・マイグレーションファイルから、データベーススキーマの包括的なドキュメントを日本語で作成してください。

カテゴリ名: {topic_name}

### スキーマ・マイグレーションファイルの内容:
{file_contents}

### 関連ファイルの要約:
{file_summaries}

以下のセクションを含む Markdown ドキュメントを作成してください。マイグレーション（config/Migrations/*.php）を時系列に適用した最終状態のスキーマを正としてください（同一テーブルへの変更は後続のマイグレーションを優先）。

# {topic_name}

## はじめに
どのフレームワーク／ORM のスキーマか、テーブル数、データベースの目的の概要。

## ER図
すべてのテーブルと主要カラム、テーブル間のリレーション（外部キー）を Mermaid の `erDiagram` で表現してください。
```mermaid
erDiagram
    AUTHORS ||--o{{ BOOKS : "has"
    BOOKS {{
        integer id PK
        string title
        integer author_id FK
    }}
```

## テーブル定義
各テーブルごとに、カラム・型・制約（NOT NULL、デフォルト値、主キー）を表で示してください。
### <テーブル名>
| カラム | 型 | 制約 | 説明 |
|------|----|------|------|
| id | integer | PK | ... |

各テーブルのインデックス（ユニーク制約含む）と外部キーも箇条書きで列挙してください。

## リレーション
テーブル間の関連（1対多・多対多の中間テーブル等）を文章で説明してください。

日本語で記述してください。テーブル名・カラム名はそのまま使用してください。ドキュメント以外の余計な前置きは書かないでください。"""

DB_SCHEMA_EXTRACT_PROMPT = """あなたはデータベース設計の専門家です。以下のスキーマ定義・マイグレーションファイルを解析し、データベーススキーマを構造化 JSON として抽出してください。マイグレーションを時系列に適用した最終状態のスキーマを抽出してください（同一テーブルへの変更は後続のマイグレーションを優先）。

### スキーマ・マイグレーションファイル:
{schema_files}

次の JSON 形式で返してください。説明文（description / overview）は日本語で記述します。**JSON のみ**を出力し、前後に余計なテキストを付けないでください:
{{
  "overview": "データベース全体の目的の概要（2〜3文、日本語）",
  "tables": [
    {{
      "name": "テーブル名（スキーマ定義のままの英字名）",
      "description": "このテーブルの役割（日本語、1〜2文）",
      "columns": [
        {{"name": "カラム名", "type": "型（integer/string/text/datetime 等）", "constraints": "PK / NOT NULL / UNIQUE / デフォルト値 等（無ければ空文字）", "description": "カラムの説明（日本語）"}}
      ],
      "indexes": [
        {{"columns": "カラム名（複合は a, b）", "unique": true, "name": "インデックス名（あれば）"}}
      ],
      "foreign_keys": [
        {{"column": "外部キーのカラム名", "references": "参照先テーブル.カラム"}}
      ]
    }}
  ],
  "relationships": [
    {{"from": "テーブルA", "to": "テーブルB", "cardinality": "1-N（または N-1 / 1-1 / N-N）", "via": "結び付ける外部キー/中間テーブル", "description": "関係の説明（日本語）"}}
  ]
}}

すべてのテーブルを漏れなく含めてください。外部キー・インデックスが無いテーブルは空配列にしてください。"""

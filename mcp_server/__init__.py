"""CodeDoc MCP server.

Exposes the graph_query_agent (Spanner code graph) to any MCP-compatible
host (Claude Code, Antigravity CLI, MCP Inspector, ...) as in-process tools
backed by an ADK Runner. See mcp_server/README.md.
"""

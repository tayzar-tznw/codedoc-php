[pytest]
testpaths = tests
markers =
    lsp: tests that require a real intelephense binary (deselect with -m "not lsp")
addopts = -q --cov=graph_generator --cov-report=term-missing --cov-fail-under=85
